# Production environment variables
# These are fallbacks - Railway will override with actual values
NODE_ENV=production
PORT=3000
MEDIA_UPLOAD_PATH=./uploads
MAX_FILE_SIZE_MB=50

# Database and Redis URLs should be set via Railway environment variables
# Do not hardcode them here for security reasons
